# 协作网页平台 — 需求文档

> **项目代号**：CoFlow  
> **版本**：v1.0  
> **日期**：2026-05-13  
> **目标客户**：企业 / 团队组织

---

## 一、项目概述

### 1.1 产品定位

CoFlow 是一款面向企业客户的 **一站式团队协作平台**，将文档协作、项目管理、在线画板、头脑风暴四大核心能力整合于统一界面，支持跨设备无缝访问，帮助团队从创意激发到项目交付实现全流程在线协作。

### 1.2 目标用户

| 用户角色 | 典型场景 |
|---------|---------|
| 企业管理者 | 分配任务、跟踪进度、审批文档 |
| 项目经理 | 创建项目、管理里程碑、自动分配任务 |
| 团队成员 | 协作编辑文档、参与头脑风暴、执行任务 |
| 设计师 | 使用在线画板进行视觉协作与原型讨论 |
| 远程/移动办公人员 | 跨设备随时访问与编辑 |

### 1.3 核心价值主张

- **零门槛协作**：打开浏览器即可开始，无需安装客户端
- **实时同步**：多人同时编辑，变更毫秒级可见
- **智能分配**：基于角色与负载自动推荐任务分配方案
- **创意无界**：画板 + 头脑风暴，让想法可视化、可沉淀
- **随处可达**：桌面、平板、手机一致体验

---

## 二、功能需求

### 2.1 多人实时文档编辑

| 功能项 | 详细说明 | 优先级 |
|-------|---------|--------|
| 富文本编辑 | 支持标题、列表、表格、代码块、引用、分割线等格式 | P0 |
| 多人协同 | 基于 OT/CRDT 算法实现多人同时编辑，光标与选区实时可见 | P0 |
| 评论与批注 | 选中文本添加评论，支持 @提及、回复、解决标记 | P0 |
| 版本历史 | 自动保存编辑历史，支持版本对比与回滚 | P1 |
| 模板系统 | 提供会议纪要、项目方案、周报等企业模板 | P1 |
| 文档权限 | 拥有者 / 可编辑 / 可评论 / 只读四级权限 | P0 |
| 文档树结构 | 支持文件夹与子文档层级组织 | P1 |
| 全文搜索 | 支持跨文档内容搜索，关键词高亮 | P1 |
| 导入导出 | 支持 Markdown、Word (.docx)、PDF 导出，Markdown/Word 导入 | P2 |
| 离线编辑 | 断网时本地缓存编辑，恢复网络后自动同步 | P2 |

### 2.2 自动任务分配与项目管理

| 功能项 | 详细说明 | 优先级 |
|-------|---------|--------|
| 项目创建 | 支持新建项目，设置名称、描述、负责人、起止时间 | P0 |
| 任务看板 | 看板视图（待办 → 进行中 → 审核 → 完成），支持拖拽 | P0 |
| 任务列表 | 列表视图，支持按优先级 / 截止日 / 负责人排序筛选 | P0 |
| 甘特图 | 时间线甘特图视图，展示任务依赖与进度 | P1 |
| 自动分配 | 基于成员角色、当前负载、技能标签自动推荐任务负责人 | P1 |
| 任务拆解 | 支持子任务、任务依赖关系、关联文档 | P1 |
| 截止提醒 | 临近截止日期 / 延期任务自动提醒（站内 + 邮件/企微通知） | P0 |
| 里程碑 | 项目关键节点设置与进度追踪 | P1 |
| 工时统计 | 成员工时填报与项目维度汇总 | P2 |
| 项目模板 | 可将已有项目保存为模板复用 | P2 |

### 2.3 在线画板

| 功能项 | 详细说明 | 优先级 |
|-------|---------|--------|
| 无限画布 | 无限缩放与平移的数字白板 | P0 |
| 绘图工具 | 画笔、直线、矩形、圆形、箭头、文本、便签 | P0 |
| 模板画板 | 流程图、思维导图、用户旅程图、看板、鱼骨图等模板 | P1 |
| 多人协作 | 多人同时绘制，光标与操作实时可见 | P0 |
| 图形库 | 预置 UI 组件、流程图符号、图标等素材 | P1 |
| 图片导入 | 支持拖拽上传图片到画板 | P1 |
| 便签功能 | 彩色便签，可拖拽分组，适合头脑风暴聚类 | P0 |
| 激光笔 | 远程演示时可用激光笔引导注意力 | P2 |
| 画板导出 | 导出为 PNG / SVG / PDF | P1 |
| 画板嵌入 | 画板可嵌入文档或项目页面 | P2 |

### 2.4 头脑风暴

| 功能项 | 详细说明 | 优先级 |
|-------|---------|--------|
| 创建房间 | 创建头脑风暴房间，邀请参与者 | P0 |
| 匿名模式 | 支持匿名提交想法，减少从众压力 | P1 |
| 计时模式 | 限时发散（如 5 分钟快速写想法），倒计时提示 | P0 |
| 想法卡片 | 参与者提交的想法以卡片形式展示 | P0 |
| 投票机制 | 对想法进行点赞/评分，自动排序 | P0 |
| 分类聚类 | 主持人可将相似想法拖拽分组 | P1 |
| 结果沉淀 | 头脑风暴结果可一键转为任务或导入画板 | P1 |
| 阶段流程 | 发散阶段 → 收敛阶段 → 决策阶段，逐步推进 | P1 |
| AI 辅助 | AI 自动归纳相似想法、生成摘要 | P2 |

### 2.6 AI 智能建议与双模型审查

| 功能项 | 详细说明 | 优先级 |
|-------|---------|--------|
| 作业结果 AI 建议 | 员工提交任务成果后，系统自动调用 AI 大模型生成改进建议、质量评估和优化方向 | P0 |
| 双模型交叉审查 | 采用两个独立大模型（主审模型 + 复审模型）对同一作业结果分别审查，交叉验证结论一致性 | P0 |
| 审查报告生成 | 双模型审查完成后自动生成结构化审查报告，包含评分、问题清单、建议项、分歧标注 | P0 |
| 审查标准管理 | 管理员可自定义审查维度、评分标准、权重配置，支持按项目/任务类型设置不同审查标准 | P1 |
| 分歧处理 | 当两个模型审查结论不一致时，高亮标注分歧点，支持人工裁决或触发第三模型仲裁 | P1 |
| 审查历史追溯 | 保留所有 AI 审查记录，支持查看每次审查的完整提示词、模型输出、人工修改轨迹 | P1 |
| 批量审查 | 支持对批量任务成果进行批量 AI 审查，生成汇总报告 | P2 |
| 自定义审查模板 | 支持创建审查提示词模板，针对不同业务场景（如代码审查、文案审核、设计评审）定制审查逻辑 | P2 |
| 审查触发规则 | 支持自动触发（任务完成时）与手动触发（按需审查）两种模式 | P1 |
| 审查结果推送 | 审查完成后自动通知被审查人及项目负责人，附审查摘要 | P1 |

### 2.7 跨设备访问

| 功能项 | 详细说明 | 优先级 |
|-------|---------|--------|
| 响应式布局 | 桌面端、平板端、手机端自适应 | P0 |
| PWA 支持 | 支持添加到主屏幕，离线缓存核心资源 | P1 |
| 原生体验 | 移动端手势操作（滑动切换、双指缩放画板） | P1 |
| 实时推送 | 跨设备消息与任务变更实时同步推送 | P0 |
| 数据一致性 | 任何设备上的操作即时同步到所有终端 | P0 |

---

## 三、非功能需求

### 3.1 性能要求

| 指标 | 目标值 |
|------|--------|
| 首屏加载时间（桌面端） | ≤ 2 秒 |
| 首屏加载时间（移动端 4G） | ≤ 3 秒 |
| 文档编辑同步延迟 | ≤ 200ms |
| 画板操作同步延迟 | ≤ 100ms |
| 单文档支持同时在线人数 | ≥ 50 人 |
| 系统可用性 | ≥ 99.9%（年停机 < 8.76h） |
| API 响应时间（P95） | ≤ 500ms |

### 3.2 安全要求

| 要求 | 说明 |
|------|------|
| 传输加密 | 全站 HTTPS / WSS |
| 数据加密 | 敏感数据（密码等）AES-256 加密存储 |
| 身份认证 | 支持账号密码 + SSO（SAML 2.0 / OAuth 2.0） |
| 权限模型 | RBAC 角色权限控制，支持自定义角色 |
| 审计日志 | 所有关键操作（登录、权限变更、文档删除等）记录审计日志 |
| 数据备份 | 每日自动备份，支持 30 天内恢复 |
| 合规性 | 符合等保 2.0 二级要求 |

### 3.3 可扩展性

- 微服务架构，各功能模块可独立部署与扩缩容
- 支持 10 人小团队至 10,000 人企业的平滑扩展
- 数据库支持读写分离与分库分表策略
- 文件存储支持对象存储（S3 / OSS），无限容量

### 3.4 兼容性

| 平台 | 最低版本 |
|------|---------|
| Chrome | 90+ |
| Firefox | 90+ |
| Safari | 15+ |
| Edge | 90+ |
| iOS Safari | 15+ |
| Android Chrome | 90+ |
| 移动端分辨率 | 375px ~ 1440px 自适应 |

---

## 四、技术要求

### 4.1 整体架构

```
┌──────────────────────────────────────────────────┐
│                    客户端层                        │
│   Web (React)  │  PWA  │  移动端适配              │
├──────────────────────────────────────────────────┤
│                    网关层                          │
│   Nginx / API Gateway（路由、限流、鉴权）           │
├──────────────────────────────────────────────────┤
│                  微服务层                          │
│  文档服务 │ 项目服务 │ 画板服务 │ 头脑风暴服务 │ AI审查服务 │ 通知服务 │
├──────────────────────────────────────────────────┤
│                  数据层                            │
│  PostgreSQL │ Redis │ MongoDB │ MinIO │ Kafka     │
├──────────────────────────────────────────────────┤
│                  基础设施                          │
│  Docker + Kubernetes │ CI/CD │ 日志 & 监控         │
└──────────────────────────────────────────────────┘
```

### 4.2 前端技术栈

| 层面 | 技术选型 | 说明 |
|------|---------|------|
| 框架 | React 18+ | 组件化开发，生态成熟 |
| 语言 | TypeScript 5+ | 类型安全，提升大型项目可维护性 |
| 构建工具 | Vite 5+ | 快速 HMR，生产构建优化 |
| 状态管理 | Zustand + React Query | 轻量全局状态 + 服务端缓存状态 |
| 文档编辑器 | 基于 ProseMirror / Tiptap | 可扩展富文本框架，支持协同 |
| 画板引擎 | 基于 Fabric.js / Konva.js + react-zoom-pan-pinch | Canvas 渲染引擎 + 无限画布缩放平移能力 |
| UI 组件库 | Ant Design 5 / Arco Design（暗色主题定制） | 企业级组件库，暗色沉浸式主题深度定制 |
| 样式方案 | Tailwind CSS + CSS Modules + Framer Motion | 原子化 + 局部作用域 + 缩放下钻动效 |
| 实时通信 | Socket.IO / WebSocket | 双向实时数据同步 |
| 图表 | ECharts | 甘特图、统计图表 |
| 离线存储 | IndexedDB + Service Worker | PWA 离线能力 |
| 国际化 | react-i18next | 预留多语言支持 |

### 4.3 后端技术栈

| 层面 | 技术选型 | 说明 |
|------|---------|------|
| 主语言 | Go / Node.js (NestJS) | 高并发 + 快速开发 |
| API 规范 | RESTful + GraphQL | 常规 CRUD 用 REST，复杂查询用 GraphQL |
| 实时通信 | WebSocket (Socket.IO) | 文档/画板协同 |
| 关系数据库 | PostgreSQL 15+ | 用户、权限、项目、任务 |
| 文档数据库 | MongoDB 7+ | 文档内容、画板数据 |
| 缓存 | Redis 7+ | 会话、实时状态、热数据缓存 |
| 消息队列 | Kafka / RabbitMQ | 异步任务、事件驱动 |
| 对象存储 | MinIO / S3 兼容 | 文件、图片、导出资源 |
| 搜索引擎 | Elasticsearch | 全文搜索 |
| 协同算法 | Yjs (CRDT) | 文档与画板协同核心算法 |
| 容器化 | Docker + Kubernetes | 部署编排 |
| CI/CD | GitHub Actions / GitLab CI | 自动构建、测试、部署 |
| 监控 | Prometheus + Grafana | 指标监控 |
| 日志 | ELK Stack | 集中式日志 |

### 4.4 协同编辑技术方案

```
客户端 A ────┐                    ┌──── 客户端 B
             │                    │
        本地 Yjs Doc         本地 Yjs Doc
             │                    │
             └──── WebSocket ─────┘
                      │
                 协同服务器 (Yjs Server)
                      │
                 持久化存储 (MongoDB)
```

- **核心算法**：采用 Yjs（CRDT 方案），相比 OT 方案无需中心化冲突解决，更适合分布式场景
- **同步策略**：增量同步，仅传输 diff；断线重连后自动合并
- **光标感知**：通过 Yjs Awareness 协议广播光标位置与用户信息

### 4.5 AI 智能建议与双模型审查技术方案

```
员工作业结果
     │
     ▼
┌──────────┐     ┌──────────────┐
│ 主审模型  │     │  复审模型     │
│ (Model A) │     │  (Model B)   │
└─────┬────┘     └──────┬───────┘
      │                 │
      ▼                 ▼
┌──────────┐     ┌──────────────┐
│ 审查结果A │     │  审查结果B    │
└─────┬────┘     └──────┬───────┘
      │                 │
      └────┬────────────┘
           ▼
     ┌───────────┐
     │ 一致性比对  │
     └─────┬─────┘
      ┌────┴────┐
      │         │
   一致       不一致
      │         │
      ▼         ▼
  生成报告   标注分歧 → 人工裁决 / 第三模型仲裁
```

**技术选型**：
- **主审模型**：DeepSeek / GLM-4 等国产大模型，侧重内容质量与合规性审查
- **复审模型**：Qwen / Claude 等大模型，侧重逻辑一致性与深度分析
- **调度服务**：独立 AI Gateway 服务，统一管理模型调用、限流、降级、计费
- **提示词管理**：审查提示词模板化存储于数据库，支持版本管理与 A/B 测试
- **一致性算法**：对两个模型的评分与结论进行差异检测，评分偏差 > 阈值或结论矛盾即标记为分歧

**审查流程**：
1. 员工提交任务成果 → 触发审查规则
2. AI Gateway 并行调用两个模型，传入相同内容 + 审查提示词模板
3. 收集两个模型输出，进行结构化解析与一致性比对
4. 一致 → 直接生成审查报告；不一致 → 标注分歧点，触发裁决流程
5. 审查报告推送至被审查人及项目负责人

### 4.6 自动任务分配算法

```
输入：任务（类型、优先级、技能标签）
      成员列表（角色、当前负载、技能评分）

算法流程：
1. 过滤：排除角色不匹配、负载已满的成员
2. 评分：对候选成员计算匹配度分数
   - 技能匹配度 × 0.4
   - 当前负载（空闲度）× 0.3
   - 历史完成率 × 0.2
   - 历史同类任务经验 × 0.1
3. 排序：按综合分数降序排列
4. 输出：推荐 Top 3 候选人，由项目经理确认或系统自动分配
```

---

## 五、风格与设计要求

> **设计灵感来源**：借鉴 [Flipbook](https://flipbook.page/) 的无限画布地图式缩放视觉风格与暗色沉浸式色彩体系，将其"深度下钻 + 像素级渲染 + 零 UI 干扰"的设计理念融入企业协作场景，打造既有未来感又兼顾专业性的协作体验。

### 5.1 设计原则

| 原则 | 说明 |
|------|------|
| **无限空间感** | 借鉴 Flipbook 地图式缩放，所有工作区（文档、画板、项目看板）均呈现为无限画布，用户可自由缩放与平移，从宏观总览到微观细节无缝切换 |
| **沉浸式暗色基调** | 默认暗色主题，深色背景让内容成为视觉焦点，减少界面干扰，支持长时间专注工作 |
| **深度下钻交互** | 点击任意元素即可深入探索其细节——文档点击段落展开批注、项目点击任务展开详情、画板点击区域放大编辑，模拟地图"放大即发现"的交互体验 |
| **极简 UI 骨架** | 隐去非必要 UI 控件，导航与工具栏在需要时浮现、闲置时自动隐藏，最大化内容展示面积 |
| **流畅过渡** | 所有缩放、下钻、页面切换均采用丝滑过渡动效，消除视觉断裂感 |
| **包容性** | 支持色彩无障碍（WCAG AA 级），键盘导航，屏幕阅读器友好 |

### 5.2 色彩系统

> 核心理念：借鉴 Flipbook 的深色沉浸式基底 + 高对比度内容层 + 发光强调色体系，在暗色画布上让协作内容"自发光"。

```
主色调 (Primary) — 深邃蓝紫，传递科技感与沉浸感
├── Primary-700  #3B1FC0  深底色，用于大面积暗色背景
├── Primary-600  #5B2FE6  主强调色，主按钮、链接、选中态
├── Primary-500  #7C4FFF  悬停态、聚焦态
├── Primary-400  #9B7AFF  辅助高亮、图标激活
└── Primary-200  #D4C4FF  极浅文字、占位符

发光强调色 (Glow Accents) — 模拟 Flipbook 可交互元素的发光提示
├── Glow-Blue    #00D4FF  信息高亮、链接标记、可下钻区域指示
├── Glow-Purple  #A855F7  画板工具激活、创意模式标识
├── Glow-Cyan    #06F9D4  实时协同光标、在线状态指示
└── Glow-White   #FFFFFF  关键操作点、焦点聚光灯效果

功能色
├── 成功  Green-400   #34D399  完成状态、审批通过（暗底上选用 400 保证可见度）
├── 警告  Amber-400   #FBBF24  临近截止、需关注
├── 错误  Rose-400    #FB7185  错误、过期、删除
└── 信息  Sky-400     #38BDF8  提示、引导

暗色模式（默认主题）
├── 背景-最深  #0A0A0F  主画布底色，近乎纯黑的深空色
├── 背景-深    #12121A  侧栏、面板底色
├── 背景-中    #1A1A25  卡片、弹窗底色
├── 背景-浅    #24243A  悬浮面板、代码块底色
├── 边框       #2E2E45  分割线、卡片边框
├── 文字-主    #F0F0F5  正文、标题
├── 文字-次    #A0A0B8  辅助说明、时间戳
├── 文字-弱    #6B6B85  占位符、禁用态

亮色模式
├── 背景-主    #F8F8FC  页面底色
├── 背景-卡片  #FFFFFF  卡片底色
├── 边框       #E2E2F0  分割线
├── 文字-主    #1A1A2E  正文
├── 文字-次    #6B6B85  辅助说明
├── 主色调     #5B2FE6  亮色模式下主色调不变
└── 强调色     #7C4FFF  亮色模式下强调色略提亮
```

### 5.3 字体

| 用途 | 字体 | 字号 | 字重 | 暗色模式色值 |
|------|------|------|------|------------|
| 大标题 | Inter / HarmonyOS Sans | 28-36px | Bold (700) | #F0F0F5 |
| 页面标题 | Inter / HarmonyOS Sans | 20-24px | Semibold (600) | #F0F0F5 |
| 卡片标题 | Inter / HarmonyOS Sans | 16-18px | Medium (500) | #E0E0F0 |
| 正文 | Inter / HarmonyOS Sans | 14px | Regular (400) | #D0D0E0 |
| 辅助文字 | Inter / HarmonyOS Sans | 12px | Regular (400) | #A0A0B8 |
| 代码 | JetBrains Mono / Fira Code | 13-14px | Regular (400) | #9B7AFF |

> 系统字体回退链：`Inter, HarmonyOS Sans, -apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", sans-serif`

### 5.4 间距与布局

> 核心理念：借鉴 Flipbook 的"内容即界面"理念，布局以无限画布为中心，UI 骨架极度精简，最大化工作区面积。

```
基础间距单位：4px
常用间距：4 / 8 / 12 / 16 / 20 / 24 / 32 / 40 / 48

页面布局（暗色沉浸式）：
┌──────────────────────────────────────────────────┐
│ 顶栏 (48px, 半透明毛玻璃)  Logo │ ··· │ 搜索 │ 头像 │
│ 默认隐藏，鼠标移至顶部区域浮现                     │
├──────┬───────────────────────────────────────────┤
│      │                                           │
│ 侧栏  │          无限画布主工作区                   │
│(220px)│                                           │
│ 半透明 │  支持自由缩放 (10% ~ 400%)                │
│ 可折叠 │  支持平移拖拽                              │
│      │  内容元素浮于深色画布之上                     │
│      │                                           │
├──────┴───────────────────────────────────────────┤
│ 底部状态栏 (28px, 半透明)  缩放比例 │ 在线人数 │ 坐标 │
└──────────────────────────────────────────────────┘

卡片/文档：圆角 12px，微弱发光边框 (border: 1px solid rgba(124,79,255,0.15))
按钮：圆角 8px，主按钮带发光投影 (box-shadow: 0 0 20px rgba(91,47,230,0.3))
输入框：圆角 8px，聚焦时发光边框 (border-color: #7C4FFF)
模态框：圆角 16px，毛玻璃背景 (backdrop-filter: blur(20px))
画布背景：深空色 #0A0A0F，可选网格点阵 (rgba(255,255,255,0.03))
```

### 5.5 缩放与下钻交互规范

> 核心交互范式：借鉴 Flipbook 的"地图缩放 + 深度下钻"模式，将协作工作区构建为可无限缩放的空间。

| 交互场景 | 操作方式 | 视觉反馈 |
|---------|---------|---------|
| 画布缩放 | 鼠标滚轮 / 双指捏合 / 底部缩放条 | 平滑缩放，带惯性，缩放中心跟随光标 |
| 画布平移 | 按住空格+拖拽 / 双指滑动 / 中键拖拽 | 抓手光标，内容平滑跟随 |
| 文档下钻 | 点击文档卡片 → 展开/放大文档编辑区 | 文档从卡片位置"生长"放大，背景虚化 |
| 任务下钻 | 点击看板任务卡片 → 展开任务详情面板 | 任务卡片展开为侧面板，带滑入动效 |
| 画板下钻 | 双击画板元素 → 放大至该区域编辑 | 平滑放大至目标区域，周围元素淡出 |
| 头脑风暴下钻 | 点击想法卡片 → 展开讨论与投票 | 卡片展开为详情视图，关联想法高亮连线 |
| 返回上层 | Esc / 面包屑导航 / 侧栏缩略图 | 平滑缩小回退，带缩放过渡 |
| 小地图导航 | 右下角迷你地图，可拖拽视口 | 视口框实时同步主画布位置 |
| 全局总览 | 缩小至 10% → 所有项目/文档鸟瞰 | 内容以缩略图矩阵呈现，可快速定位跳转 |

**缩放级别定义**：

```
10%-25%  ──  鸟瞰视图：显示所有工作区缩略图，快速导航
25%-50%  ──  总览视图：项目看板、文档列表、画板缩略图
50%-100% ──  标准视图：正常编辑与阅读模式（默认 100%）
100%-200%──  细节视图：精细编辑，文字排版微调
200%-400%──  微观视图：画板像素级编辑，文档逐字校对
```

### 5.6 动效规范

| 场景 | 动效 | 时长 | 缓动 |
|------|------|------|------|
| 画布缩放 | 平滑缩放 + 惯性 | 300ms | cubic-bezier(0.25, 0.1, 0.25, 1) |
| 下钻展开 | 元素从原位放大 + 背景虚化 | 400ms | cubic-bezier(0.34, 1.56, 0.64, 1) |
| 退出下钻 | 缩小回原位 + 背景恢复 | 300ms | cubic-bezier(0.4, 0, 0.2, 1) |
| 页面切换 | 3D 推移（左出右入） | 350ms | ease-in-out |
| 面板滑入 | 从边缘滑入 + 毛玻璃淡入 | 250ms | ease-out |
| 工具栏浮现 | 顶部/底部工具栏淡入 | 150ms | ease-out |
| 工具栏隐藏 | 工具栏淡出 | 300ms | ease-in |
| 列表项增删 | 滑入/滑出 + 发光闪烁 | 300ms | ease-in-out |
| 协同光标 | 平滑跟随 + 彩色尾迹 | 80ms | ease-out |
| 按钮 Hover | 背景色过渡 + 微弱发光增强 | 150ms | ease |
| 发光脉冲 | 可交互元素的呼吸发光提示 | 2000ms 循环 | ease-in-out |

> **原则**：动效服务于空间感知与操作反馈，缩放与下钻动效需传达"空间纵深"的视觉隐喻；尊重 `prefers-reduced-motion` 系统设置，减弱模式下仅保留必要过渡。

### 5.7 图标与插图

- **图标库**：使用 Phosphor Icons（线性风格），在暗色模式下使用 400 线宽，图标色值 #A0A0B8，激活态 #7C4FFF
- **图标尺寸**：20px / 24px / 28px 三档
- **可交互提示**：可下钻的元素在 Hover 时图标右下角显示 ↗ 小箭头或 🔍 放大标识
- **插图**：使用深空 + 发光粒子风格，主色调蓝紫色系，用于空状态、引导页、加载占位
- **加载动画**：采用粒子汇聚/散开动画，而非传统转圈 spinner
- **头像**：默认首字母头像（深色底 + 发光色字母），在线状态用 Glow-Cyan 小圆点

### 5.8 暗色模式（默认主题）

- 默认启动即为暗色模式，亮色模式为可选切换
- 切换时全局平滑过渡（400ms，含背景渐变）
- 画布背景采用深空色 #0A0A0F，可选网格点阵辅助定位
- 所有卡片、面板采用半透明毛玻璃效果，营造空间层次感
- 发光强调色仅用于关键交互点，克制使用避免视觉噪音
- 文字层级通过亮度区分而非色相，保证暗色下可读性

### 5.9 Flipbook 风格映射到企业协作场景

| Flipbook 核心特征 | CoFlow 适配方案 |
|------------------|----------------|
| 无限画布 / 像素级渲染 | 所有工作区基于无限画布，文档/看板/画板均为画布上的可缩放元素 |
| 点击下钻深度探索 | 点击任何元素（任务、文档段落、画板区域）展开细节，逐层深入 |
| 深色沉浸式背景 | 深空色画布底色，内容浮于其上"自发光" |
| Zero UI / 极简骨架 | 导航栏/工具栏自动隐藏，鼠标靠近边缘时浮现，最大化工作区 |
| 发光强调可交互元素 | 可下钻/可编辑元素带微弱发光边框，Hover 时增强发光 |
| 实时像素流过渡 | 下钻/返回切换采用平滑缩放过渡，消除页面跳转断裂感 |
| 小地图全局导航 | 右下角迷你地图，总览全局，快速定位跳转 |

---

## 六、页面与模块规划

### 6.1 页面清单

| 页面 | 路由 | 核心功能 |
|------|------|---------|
| 登录/注册 | /login, /register | 账号登录、SSO、注册 |
| 工作台首页 | /dashboard | 待办概览、最近文档、项目进度、通知 |
| 文档中心 | /docs | 文档树、搜索、最近编辑 |
| 文档编辑 | /docs/:id | 富文本编辑、多人协同、评论 |
| 项目列表 | /projects | 项目卡片、筛选、新建 |
| 项目详情 | /projects/:id | 看板/列表/甘特图、任务管理 |
| 画板列表 | /boards | 画板卡片、模板选择 |
| 画板编辑 | /boards/:id | 无限画布、绘图工具 |
| 头脑风暴 | /brainstorm | 创建/加入房间 |
| 头脑风暴房间 | /brainstorm/:id | 发散、收敛、投票 |
| 团队管理 | /team | 成员管理、角色权限 |
| 个人设置 | /settings | 个人信息、通知偏好 |

### 6.2 导航结构

```
顶栏导航（全局）：
  Logo │ 工作台 │ 文档 │ 项目 │ 画板 │ 头脑风暴 │ ··· │ 搜索 🔍 │ 通知 🔔 │ 头像

侧栏导航（上下文相关）：
  - 文档页 → 文档树 / 收藏 / 最近 / 回收站
  - 项目页 → 项目列表 / 我参与的 / 我负责的
  - 画板页 → 我的画板 / 共享画板 / 模板
```

---

## 七、数据模型（核心）

### 7.1 用户与组织

```
User: id, name, email, avatar, role, status, created_at
Organization: id, name, plan, settings, created_at
OrgMember: org_id, user_id, role(owner/admin/member/guest), joined_at
```

### 7.2 文档

```
Document: id, org_id, title, content(Yjs Doc), parent_id, creator_id, 
          status, tags, created_at, updated_at
DocPermission: doc_id, user_id, role(owner/editor/commenter/viewer)
DocComment: id, doc_id, user_id, content, range, parent_id, resolved, created_at
DocVersion: id, doc_id, content_snapshot, creator_id, created_at
```

### 7.3 项目与任务

```
Project: id, org_id, name, description, owner_id, status, start_date, 
         end_date, settings, created_at
Task: id, project_id, title, description, assignee_id, reporter_id, 
      status, priority, tags, start_date, due_date, effort_hours, 
      parent_id, created_at, updated_at
TaskDependency: task_id, depends_on_task_id, type(FS/FF/SF/SS)
Milestone: id, project_id, name, due_date, status
```

### 7.4 画板

```
Board: id, org_id, title, data(Yjs Doc), creator_id, template_type, 
       thumbnail, created_at, updated_at
BoardPermission: board_id, user_id, role
BoardElement: id, board_id, type, data, position, creator_id, created_at
```

### 7.5 AI 审查

```
AIReview: id, task_id, doc_id, trigger_type(auto/manual), status(pending/running/completed/failed),
          created_at, completed_at
AIReviewResult: id, review_id, model_name, model_version, prompt_template_id,
                score, summary, issues(JSON), suggestions(JSON),
                raw_output, created_at
AIReviewDivergence: id, review_id, dimension, model_a_value, model_b_value,
                    resolution(manual/arbitration/resolved), resolved_by, resolved_at
ReviewTemplate: id, org_id, name, description, dimensions(JSON), scoring_rules(JSON),
                prompt_template, created_by, created_at, updated_at
```

### 7.6 头脑风暴

```
BrainstormRoom: id, org_id, title, creator_id, mode(open/anonymous), 
                phase(diverge/converge/decide), timer_duration, status, created_at
BrainstormIdea: id, room_id, user_id(nullable if anonymous), content, 
                votes, group_id, created_at
BrainstormVote: idea_id, user_id, score, created_at
```

---

## 八、接口规范（核心 API）

### 8.1 RESTful API 示例

```
# 文档
GET    /api/v1/docs                    # 文档列表
POST   /api/v1/docs                    # 创建文档
GET    /api/v1/docs/:id                # 获取文档
PUT    /api/v1/docs/:id                # 更新文档元信息
DELETE /api/v1/docs/:id                # 删除文档
GET    /api/v1/docs/:id/versions       # 版本列表

# 项目与任务
GET    /api/v1/projects                # 项目列表
POST   /api/v1/projects                # 创建项目
GET    /api/v1/projects/:id/tasks      # 任务列表
POST   /api/v1/projects/:id/tasks      # 创建任务
PUT    /api/v1/tasks/:id/assign        # 分配任务
POST   /api/v1/tasks/:id/assign/auto   # 自动推荐分配

# 画板
GET    /api/v1/boards                  # 画板列表
POST   /api/v1/boards                  # 创建画板
GET    /api/v1/boards/:id              # 获取画板

# 头脑风暴
POST   /api/v1/brainstorm/rooms        # 创建房间
GET    /api/v1/brainstorm/rooms/:id     # 房间详情
POST   /api/v1/brainstorm/rooms/:id/ideas  # 提交想法
POST   /api/v1/brainstorm/rooms/:id/vote   # 投票

# AI 审查
POST   /api/v1/reviews                  # 发起审查（自动/手动）
GET    /api/v1/reviews/:id              # 获取审查详情
GET    /api/v1/reviews/:id/report       # 获取审查报告
POST   /api/v1/reviews/:id/resolve      # 分歧裁决
GET    /api/v1/review-templates         # 审查模板列表
POST   /api/v1/review-templates         # 创建审查模板
PUT    /api/v1/review-templates/:id     # 更新审查模板
POST   /api/v1/reviews/batch            # 批量审查
```

### 8.2 WebSocket 事件

```
# 文档协同
doc:join        { docId, userId }          # 加入文档
doc:leave       { docId, userId }          # 离开文档
doc:update      { docId, delta, userId }   # 文档变更
doc:cursor      { docId, cursor, userId }  # 光标移动

# 画板协同
board:join      { boardId, userId }
board:update    { boardId, delta, userId }
board:cursor    { boardId, position, userId }

# 通知
notification    { type, title, body, link } # 实时通知推送
```

---

## 九、部署与运维

### 9.1 部署架构

- **容器化**：所有服务 Docker 化，通过 Kubernetes 编排
- **网络**：Ingress → Service → Pod 三层架构
- **存储**：数据库独立 StatefulSet，对象存储使用 MinIO / 云 OSS
- **CDN**：静态资源通过 CDN 加速分发

### 9.2 环境规划

| 环境 | 用途 | 数据 |
|------|------|------|
| Development | 开发调试 | 模拟数据 |
| Staging | 集成测试 | 匿名化生产数据 |
| Production | 正式运行 | 真实数据 |

### 9.3 监控告警

- **基础设施**：CPU / 内存 / 磁盘 / 网络 监控
- **应用层**：API 响应时间 / 错误率 / WebSocket 连接数 / 并发编辑人数
- **业务层**：日活 / 文档创建数 / 任务完成率
- **告警**：P0 故障电话告警，P1 问题企微/邮件通知

---

## 十、里程碑规划

| 阶段 | 时间 | 交付内容 |
|------|------|---------|
| **M1 — 基础框架** | 第 1-4 周 | 技术架构搭建、UI 组件库、登录注册、基础路由 |
| **M2 — 文档协作** | 第 5-8 周 | 富文本编辑器、CRDT 协同、评论批注、权限管理 |
| **M3 — 项目管理** | 第 9-12 周 | 任务看板、列表、甘特图、自动分配算法、提醒通知 |
| **M3.5 — AI 审查** | 第 12-14 周 | AI Gateway 服务、双模型审查流程、审查报告生成、分歧处理、审查标准管理 |
| **M4 — 画板** | 第 13-16 周 | 无限画布、绘图工具、多人协同画板、模板 |
| **M5 — 头脑风暴** | 第 17-19 周 | 房间管理、想法提交、投票聚类、结果沉淀 |
| **M6 — 整合优化** | 第 20-22 周 | 跨模块联动、PWA、暗色模式、性能优化 |
| **M7 — 测试上线** | 第 23-24 周 | 安全审计、压力测试、灰度发布、正式上线 |

---

## 十一、风险与应对

| 风险 | 影响 | 应对措施 |
|------|------|---------|
| CRDT 协同性能瓶颈 | 大文档多人编辑卡顿 | 分片同步 + 虚拟化渲染 + 增量更新 |
| WebSocket 连接不稳定 | 协同中断 | 自动重连 + 离线缓存 + 冲突合并 |
| 自动分配算法准确度低 | 任务分配不合理 | 初期以推荐为主，积累数据后迭代模型 |
| 跨端体验一致性 | 移动端体验打折 | 移动端优先设计关键路径，画板简化操作 |
| 数据安全与隐私 | 企业客户信任度 | 等保合规 + 私有化部署选项 + 透明审计 |

---

## 附录 A：术语表

| 术语 | 说明 |
|------|------|
| CRDT | Conflict-free Replicated Data Type，无冲突复制数据类型 |
| OT | Operational Transformation，操作转换算法 |
| PWA | Progressive Web App，渐进式 Web 应用 |
| RBAC | Role-Based Access Control，基于角色的访问控制 |
| SSO | Single Sign-On，单点登录 |
| SAML | Security Assertion Markup Language，安全断言标记语言 |

---

## 附录 B：参考竞品

| 产品 | 借鉴点 |
|------|--------|
| Notion | 文档 + 数据库一体化体验 |
| Figma | 实时协同画板的流畅体验 |
| Miro | 无限画布与便签聚类 |
| Linear | 项目管理的极简交互与快捷键 |
| 飞书文档 | 中文富文本编辑与评论体验 |
| GitMind | 头脑风暴的流程化设计 |

---

> 本文档为 CoFlow 协作平台 v1.0 需求基线，后续迭代将基于用户反馈与业务发展持续更新。
